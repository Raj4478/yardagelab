# Canonical production origin (used for <link rel=canonical>, sitemap, JSON-LD).
# No trailing slash.
NEXT_PUBLIC_SITE_URL=https://yardagelab.com

# Set to "false" to force noindex even on a production deploy (optional).
# NEXT_PUBLIC_ALLOW_INDEX=false
